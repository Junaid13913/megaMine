"""
megaMine v2.0
Hybrid literature mining framework for precision oncology.

Modules:
    core/extractor.py     — main extraction engine
    modules/temporal.py   — temporal evidence tracking
    modules/contradiction.py — contradiction detection
    modules/graph.py      — knowledge graph export
    modules/trials.py     — ClinicalTrials linkage
    modules/llm_verify.py — LLM verification layer
    validation/gold_standard.py — gold standard dataset
    validation/evaluation.py    — metrics computation
    validation/benchmarks.py    — system comparison
"""

__version__ = "2.0.0"
__author__  = "Muhammad Junaid"
